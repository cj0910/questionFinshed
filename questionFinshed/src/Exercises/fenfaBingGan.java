package Exercises;

import java.util.Arrays;

//455. 分发饼干
public class fenfaBingGan {
}

class Solution_findContentChildren {
    public int findContentChildren(int[] g, int[] s) {
        Arrays.sort(g);
        Arrays.sort(s);
        int start = 0;
        int count = 0;
        for(int i=0;i<s.length && start<g.length;i++){
            
        }
        return  count;
    }
}